# Changelog

## v0.5.0
- Change the API extension module to link request nodes directly to entity nodes.
- Fix default namespaces (like rdf, rr, etc.) are not always added in the generated RML mapping file.

## v0.4.0